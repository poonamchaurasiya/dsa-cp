#include<iostream>
int count ones(int arr[],int n)
{
    int low =0;,high =n-1;
    while(low<=high)
    {
        int mid =(low +high)/2;
        if (arr[mid]==0)
        low= mid+1;
        else{
            if (mid ==0:: arr[mid]!arr[mid])
            return(n-mid);
        }
    }
}
return 0;
