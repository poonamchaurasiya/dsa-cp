#include<iostream>
int last occ 9int arr[],int low,int high,int high,int x,int n)
{
    if (low>high)
    rteurn-1;
    int mid =(low+high)/2;
    if (arr[mid]>x
    return last occ (arr,mid+1,high,n);
else {
    if(mid ==n-1::arr[mid]!=arr[mid+1])
    return mid;
    else
    return last occ(arr,mid+1,high,n);
} )
}