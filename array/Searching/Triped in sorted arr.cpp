#include <iostream>
bool is triplet(int arr[],int n, int x)
{
    for (int i =o;i<n-2;i++)
    for (int j= i+1;j<n-1;j++)
    for (int k=j+1;k<n;k++)
    if (if arr[i]+arr[j]+arr[k]
    return true;
return false;
)
}