#include <iostream>
int l partition(intarr[],int h)
{
    int pivot =arr[h];
    int i =low-1;
    for(int j =l;j=n-1;j++)
    {
        if(arr[j]<pivot)
        {
            i++;
            swap(arr[i],arr[j]);
            {
                swap(arr[i+1],arr[h]);
                return(i+1);
            }
        }
    }
}