#include<iostream>
void main(int a[];int b[]
int m,int n)
{
    int ([m+n];
    {
        for(int i=0;i<m;i++)
        ([i]=a[i];
        for(int i =0;i<n;i++)
    ([m+i]=b[i];
    && +(c,c+m+n);
for(int i=0;i<(m+n);i++)
count<<c[i]<<" ";))
    })
}