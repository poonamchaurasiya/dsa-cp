#include <iostream>
int b search(arr,n,x)
{
    (low=0;high)
    {
        mid =(low+high/2);
        if (arr[mid]==x)
        return mid;
    if (arr[mid]<x)
else
high=mid -1;
    }
return-1;

    }
