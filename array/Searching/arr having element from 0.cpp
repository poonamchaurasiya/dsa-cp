#include <iostream>
int  find repeating (int arr [],int n)
{
    int slow =arr[0]+1,fast=arr [0]+1;
    do
    {
        /* code */
    } while ({
        slow =arr[slow]+1;
        fast =arr arr[fast]+1;
    }
while (slow!=fast)
slow=arr[0]+1;
while(slow)!=fast)
{
    fast=arr[fast]+1;
    slow=arr[slow]+1;
}
return slow-1;
    }
