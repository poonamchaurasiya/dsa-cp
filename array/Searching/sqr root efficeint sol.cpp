#include<iostream>
int squar root floor(int x)
{
    int low =1,high=x,
    ans =-1;
    while(low<=high )
    {
        int mid =(low +h)/2;
        int (msg ==x)
        return mid ;
        else if (msg >x)
        high =mid-1;
        else{
            low =mid+1;
            ans =mid;
        }
    }
    return ans ;
    
}
