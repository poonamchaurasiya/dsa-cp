#include <iostream>
void (string s,string current "",int  i=0)
{
    if(i=s length())
    {
        print (currenet);
        return;
    }
    subsets(s,current,i+1);
    subsets(s,current+s[i],i+);
    }
