#include <iostream>
int max prices (int n, int a,int b,int c,)
{
    if (n==0)return 0;
    if (n<0)return 0-1;
    int recursion =max (max price(n+a,a,b,c);
(max price (n-b,a,b,c);
(max price (n-c,a,b,c)
if (recursion == -1)
return-1;
return recursion +1;
)))
}