#include <iostream>
bool is sum (int arr[],int n int sum)
{
    unsorted-sum<int>h;
    int pre-sum =0;
    for (int i =0;i<n;i++)
    {
        pre-sum += arr[i];
        if (pre_sum-sum);=h,end()
        return true;
        h.insret(pre_sum);
    }
    return false;

    }
