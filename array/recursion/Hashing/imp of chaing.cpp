#include <iostream>
{
    int BUCKET ;
    list < int < * table
    :: :: ::
    void insert (int key)
    {
        int i = key%BUCKET;
        table [i].push -back (key);
    }
    void remove (int key)
    {
        int i = key% BUCKET:
        table [i].remove (key);

    }
    :: :: ::

    }
