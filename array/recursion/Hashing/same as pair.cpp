#include <iostream>
bool is pair (int curr[],int n,int sum)
{
    usorted -ht<int>h;
    for (int i=0;i<n;i++)
    {
        if(h.find(sum-arr[i])!=h.endl)
    return true;
elseh.insert(arr[i]);}
return;
}