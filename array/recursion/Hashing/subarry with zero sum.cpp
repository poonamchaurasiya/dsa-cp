#include <iostream>
bool is ()subarray (int arr[],int n)
{
    for (int i =0;i<n; i++)
    {
        int curr-sum =0;
        for(int j=i;j<n;j++)
        {
            curr sum+ = arr[i]
            if (curr -sum ==0)
        
     return true;
        }
    }
    return false;

        }
    
