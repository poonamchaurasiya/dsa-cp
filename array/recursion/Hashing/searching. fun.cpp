#include <iostream>
{
    int BUCKET ;
    list <int> * table  
    ::  ::  ::
    bool search (int key)
    {
        int i = key % BUCKET;
        for (auto x; table [i])
        if (x == key)
        return true ;
        return false ;
    }
    :: :: ::
}