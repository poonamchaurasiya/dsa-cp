#include < iostream>
using namespace std ;
void leftRoatete (int arr[],int n)
{
    int temp[d];
    for (int i=0;i<n;i++)
    temp[i] =arr [i];
    arr [i-d]= ar[i];
    for (int i=0; i<d,i++)
    arr[n-d+i]=temp[i];    
}